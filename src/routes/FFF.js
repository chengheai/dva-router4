// import { connect } from 'dva';
import React, { Component } from 'react';

class FFF extends Component {
  render() {
    return (
      <div>
        FFF
      </div>
    )
  }
}

FFF.propTypes = {
};

// export default connect()(FFF);
export default FFF
